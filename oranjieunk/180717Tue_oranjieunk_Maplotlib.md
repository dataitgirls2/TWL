matplotlib
- 불러와야 한글 폰트, 속성을 지정할 수 있음
- matlab 기반
seaborn.pydata.org -> matplotlib 기반으로 만들어짐
plotnine : ggplot 문법 기반으로 만들어짐

NaN : null 값
각 row가 serial data가 됨
df2.<TAB>나 df2.describe?를 사용하면 사용할 수 있는 명령어 볼 수 있음
axis = 0은 행, axis = 1은 열