classification, clustering, regression

사이킥런 : 파이썬 기계학습의 대가
자연어 처리는 전처리가 중요

startwith, endwith

feature selection : 뽑아오는 조건(column)이 무엇일지

generalization(일반화) : outlier, missing data를 없애는 것
clf.fit(X_train, y_train)대문자와 소문자는 행렬과 데이터의 차이
random forest로 예측하기!
x_train과 x_test는 column이 같아야 학습 가능!
비지도 학습 : label이 없음

word2vec은 추천 시스템에도 활용됨!
pandas, numpy는 데이터 전처리에 굉장히 중요함
perceptron
메모리, GPU 성능이 향상되고 장비가 발달 + 수집한 데이터가 딥러닝의 유행에 한 몫