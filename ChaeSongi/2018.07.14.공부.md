# 사용자 정의 함수
```
ef 함수명:
"함수 설명"


return 반환값
```
```
def find_max(a,b):
    if a>b:
        y=a
    else:
        y=b
    return y
find_max(5,2)
find_max(2,5)
```
## 전역변수와 지역변수
```
def test():
    print(a)
a = 100 # 전역변수
print(a)
test()
print(a)
```


