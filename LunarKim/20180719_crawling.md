# 20180719_crawling.

> 과제 점검

```replace```함수를 쓰면 데이터에 필요없는 것들을 제거할 수 있다. 이를 cleansing이라고 한다. 

- 예인님이 텍스트 뽑아내신 for문을 이용해서 제목을 뽑아내보자. 

  : tag_list가 비어있으면 []를 return하는게 가장 합리적이다. 

  그냥 text를 뽑아내는 방법이 있다. ```.strip```

  if / else 문에서 1단계만 다르고 2단계만 다르면 2단계를 if문 밖으로 빼도된다. 

- 김예은님 과제에 div_tag뽑는 방법이 있음. 

> 크롤링의 쓸모.

범죄가 일어나는 사이트를 크롤링 해놓았다가 이상한 게 나타나면 자동으로 PDF를 떠놓을 수 있다.  PDF를 추적해봤을 때 한 달뒤에 삭제 되는 것들은 장기보관, 아닌 것들은 삭제해놓을 수 있다. 

> CSS 선택자(SELECTOR)로 원하는 정보 뽑아내기.

- css : 관심사의 분리의 한 사례이다. 

html과 css의 분리. css는 html은 그대로 놔두고도 사이트를 자유자재로 스타일을 변경할 수 있다. 덕분에 html에서 crawling이 쉬워졌다.  

우리는 css를 이용해서 검색을 할 것임.  

- 자주 쓰이는 선택자들 : 

엘리먼트 선택자 : null 

ex) p -> 태그 이름이 p인 엘리먼트

아이디 선택자 : #

ex) #welcome -> id 속성이 welcome인 엘리먼트

클래스 선택자 : .

ex).highlight -> 클래스가 highlight임. 

- 선택자의 조합 :

붙여쓰는 경우 : 한 세대에서

선택자 사이에 띄어쓰기(descendant  selector라고 부른다.) 를 넣는 경우. 앞에 있는 것의 자손들 중 속성이 뒤에있는 것인 엘리먼트 

ex1) body .highlight 

 body 의 자손들중에 highlight태그가 있는애들을 찾는다. 

ex2)

찾을 때 ```>``` 직계 자식만 찾는다. 

- attrib 는 key = "value"형식이다.  이 키는 다 다른 역할을 한다. 

  id = 번호같은 것이라 하나만 있을 수 있다. 

>  list comprehension 공부 

browser에서 selector를 찾아볼 수 있다. 

대표성이 있는 샘플을 만들려면 과거부터 쌓여있는 자료를 보는 것이 좋다. 